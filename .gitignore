node_modules
.next
out
build
.env*
.DS_Store
Thumbs.db
.vercel
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------------------------------------------------------------------------
# 30MB source master. The site does NOT use it — public/video/hero-scrub.mp4
# is the all-keyframe 12MB encode that LandscapeScrollHero actually plays.
# Committing it would pin 30MB into git history permanently.
# Delete this line if you want the master tracked too.
# ---------------------------------------------------------------------------
/public/video/Hero.mp4
